===精灵===: 常量程序
造型数: 1, 音效数: 0, 积木数: 127


=== 脚本 ===

--- 定义 自定义过程 ---
定义 自定义过程
  删除 休息变量 的全部项目
  设 n 为 ?
  设 m用于“将变量添加到列表” 为 ?
  如果 (mh) 那么
  否则

--- 当绿旗被点击 ---
当绿旗被点击
设 常数_赤道半径R 为 ?
删除 緯度経度基準点_緯度 的全部项目
删除 緯度経度基準点_経度 的全部项目
删除 緯度経度基準点_x 的全部项目
删除 緯度経度基準点_y 的全部项目
删除 緯度経度基準点_地名 的全部项目
将 ? 加入 緯度経度基準点_緯度
将 ? 加入 緯度経度基準点_経度
将 ? 加入 緯度経度基準点_x
将 ? 加入 緯度経度基準点_y
将 ? 加入 緯度経度基準点_地名
将 ? 加入 緯度経度基準点_緯度
将 ? 加入 緯度経度基準点_経度
将 ? 加入 緯度経度基準点_x
将 ? 加入 緯度経度基準点_y
将 ? 加入 緯度経度基準点_地名
将 ? 加入 緯度経度基準点_緯度
将 ? 加入 緯度経度基準点_経度
将 ? 加入 緯度経度基準点_x
将 ? 加入 緯度経度基準点_y
将 ? 加入 緯度経度基準点_地名
将 ? 加入 緯度経度基準点_緯度
将 ? 加入 緯度経度基準点_経度
将 ? 加入 緯度経度基準点_x
将 ? 加入 緯度経度基準点_y
将 ? 加入 緯度経度基準点_地名
将 ? 加入 緯度経度基準点_緯度
将 ? 加入 緯度経度基準点_経度
将 ? 加入 緯度経度基準点_x
将 ? 加入 緯度経度基準点_y
将 ? 加入 緯度経度基準点_地名
将 ? 加入 緯度経度基準点_緯度
将 ? 加入 緯度経度基準点_経度
将 ? 加入 緯度経度基準点_x
将 ? 加入 緯度経度基準点_y
将 ? 加入 緯度経度基準点_地名

--- 当接收到 [Reset_旗押してもリセットされないやつ] ---
当接收到 [Reset_旗押してもリセットされないやつ]
设 常数_观测点纬度 为 ?
设 常数_观测点经度 为 ?
设 常数_观测点-表层地基放大度 为 ?
设 n 为 ?
删除 观测点_纬度 的全部项目
删除 观测点_经度 的全部项目
删除 观测点_表层地基放大度 的全部项目
変数をリストへ %s %s
変数をリストへ %s %s
変数をリストへ %s %s